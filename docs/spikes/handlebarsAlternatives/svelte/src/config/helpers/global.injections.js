export const GLOBAL_STATIC_IMPORTS = [
	`import { camelCase, pascalCase, kebabCase, snakeCase, lowerCase } from 'case-anything';`
]


/**
 * @type {never[]}
 */
export const GLOBAL_DYNAMIC_IMPORTS = [
	// Dynamic import example:
	// if (code.includes('pfPrefix')) {
	//   dynamicImports.push("import { pfPrefix } from '$scripts/helpers';");
	// }
];